service:
  name: deploy
  identifier: deploy
  serviceDefinition:
    type: Kubernetes
    spec:
      manifests:
        - manifest:
            identifier: deploy
            type: K8sManifest
            spec:
              store:
                type: Github
                spec:
                  connectorRef: account.GIT
                  gitFetchType: Branch
                  paths:
                    - servicedeploy.yaml
                  branch: master-demo-patch
              skipResourceVersioning: false
              enableDeclarativeRollback: false
  gitOpsEnabled: false
  orgIdentifier: default
  projectIdentifier: ias_template
